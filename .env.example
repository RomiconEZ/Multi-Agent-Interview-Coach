# -----------------------------
# Compose-level configuration
# -----------------------------
COMPOSE_PROJECT_NAME=compose_project_name   # Base name for containers and network

# -----------------------------
# Networking and ports
# -----------------------------
BACKEND_PORT=8000          # Exposed backend port
NGINX_EXTERNAL_PORT=90   # Host port for Nginx
REDIS_CACHE_PORT=6379      # Exposed Redis port

# -----------------------------
# Redis cache settings
# -----------------------------
REDIS_CACHE_HOST=redis_cache  # Service name in docker-compose network
REDIS_CACHE_PORT=6379         # Redis TCP port

# -----------------------------
# Application metadata
# -----------------------------
APP_NAME=APP_NAME
APP_DESCRIPTION=APP_DESCRIPTION
APP_VERSION=0.1.0
LICENSE_NAME=Proprietary
CONTACT_NAME=Owner
CONTACT_EMAIL=owner@example.com

# -----------------------------
# Environment selection
# Allowed values: local | staging | production
# -----------------------------
ENVIRONMENT=local

# -----------------------------
# Client-side caching
# -----------------------------
CLIENT_CACHE_MAX_AGE=60   # Seconds for Cache-Control: public, max-age=...

# -----------------------------
# Logging
# -----------------------------
APP_TZ_OFFSET=3           # Hours offset for log timestamps
APP_LOG_DIR=/code/logs    # Directory for system.log and personal.log

# -----------------------------
# Runtime tuning
# -----------------------------
RESERVED_CPUS=6           # CPUs to reserve from Gunicorn worker calculation